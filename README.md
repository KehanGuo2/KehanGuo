# kehanguo2.github.io/KehanGuo

Personal academic homepage of Kehan Guo. Plain static site: one `index.html`, one `style.css`, no build step (`.nojekyll` disables Jekyll on GitHub Pages).

To update content, edit `index.html` directly. Spans with class `placeholder` mark content still to be filled in — remove the class once filled.
